# Optional Environment Variables
# Most variables are now directly in docker-compose.yml
# This file is kept for reference and optional overrides

# Hugging Face Token (optional, for private models)
HUGGING_FACE_HUB_TOKEN=

# Override these in docker-compose.yml for production
# POSTGRES_USER=root
# POSTGRES_PASSWORD=password
# POSTGRES_DB=n8n

# N8N Secrets (configured in docker-compose.yml)
# N8N_ENCRYPTION_KEY=super-secret-key
# N8N_USER_MANAGEMENT_JWT_SECRET=even-more-secret

# Jira Configuration (configured in docker-compose.yml)
# JIRA_PERSONAL_TOKEN=your_personal_access_token
# JIRA_PROJECT=project_key
# JIRA_USERNAME=your_jira_username
